# Prompt for Claude Design (or any design agent)

Attach this repo (or just this folder) and paste everything below the line.

---

You're exploring visual variants for my personal site, noahairmet.com. The
site is already built and shipped — a small Astro static site (home,
"field notes" blog, 404) whose whole visual system lives in
`src/styles/site.css`. I want design exploration on top of the existing
structure and content, not a rebuild.

Read `design-kit/CONTEXT.md` first: it holds the brief, the complete
verbatim copy, and the hard constraints. The short version: the direction
is "alpine field notes" (a survey-plate reading of butterfly.so/pond),
the copy is final and may not be added to, and the site must stay
zero-JavaScript, fully self-hosted under a strict CSP, with at most one
ongoing ambient motion.

Give me 2–3 distinct visual variants of the home page and one post page.
For each: what changed, why it still reads "honest student who hikes",
and the tradeoff versus the current design. Worth exploring: contour art
density and placement, chip color usage, the Besley/Literata balance, the
links-row treatment. I care about restraint and precision, not more
decoration — anything that reads "startup landing page" is off-brief.
